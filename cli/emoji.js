const emoji = require('node-emoji');

module.exports = {
    bird: [
        emoji.get('bird'),
        emoji.get('baby_chick'),
        emoji.get('hatching_chick')
    ],
    cat: emoji.get('cat')
};